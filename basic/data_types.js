"use strict" //treats all JS code as newer version.
// alert(3+3) // we are using node.js so alert won't work properly as it isn't browser.

/* Data types in Javascript
    number => 2 power 53
    bigint
    string => ""
    boolean => true/false
    null => standalone value
    undefined => value not defined or given
    symbol => unique
    
    
    object*/

    console.log(typeof undefined);
    